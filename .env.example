# ---- Supabase ----
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=
SUPABASE_DB_URL=

# ---- Business ----
NEXT_PUBLIC_WHATSAPP_NUMBER=
# The live domain. Canonical URLs, the sitemap, robots.txt and every
# structured-data block are built from this — set it in production.
NEXT_PUBLIC_SITE_URL=

# ---- SEO (optional) ----
# The token from Google Search Console → verify by HTML tag.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# ---- Admin convenience ----
NEXT_PUBLIC_ADMIN_EMAIL=
# local dev only — do NOT set in production
NEXT_PUBLIC_DEV_ADMIN_PASSWORD=
